{
  "name": "pi-agent-rss",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "RSS aggregation plugin: add/fetch/unread/search/markread/list/remove, optional scheduled fetch",
  "keywords": ["pi-package"],
  "scripts": {
    "check": "echo 'nothing to check'"
  },
  "pi": {
    "extensions": ["./index.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "typebox": "*"
  }
}